<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, minimal-ui">
    <title>OccupancyDB</title>
    <link rel="stylesheet" type="text/css" href="/Libraries/semantic/out/semantic.min.css">
    <link rel="stylesheet" type="text/css" href="/Pages/Home/index.css">
    <script src="/Libraries/jquery-3.1.1.js"></script>
    <script src="/Libraries/semantic/out/semantic.min.js"></script>
    <!-- Always put our JS last so that if our JS calls something from another JS file, it's already available -->
    <script src="/Pages/Home/index.js"></script>
</head>

<body onload="init(<%- (darkMode) %>)" style="display: none">

    <!--  Menu -->
    <div class="ui top menu">
        <!-- Conditionally draw the account item -->
        <% if (user) { %>
        <div class="item" onclick="location.href='/user'">
            <i class="ui large user circle icon"></i> <%= user.email %>
        </div>
        <% } else { %>
        <div class="item" onclick="location.href='/login?origin=/'">
            <i class="ui large user icon"></i> Log In
        </div>
        <% } %>
        <div class="active <%= color %> item" onclick="location.href='/'">
            <i class="ui home icon"></i> Main
        </div>
    </div>

    <div class="ui container" style="height: 90%; display: flex;">
        <div style="width: 100%; display: flex; flex-direction: column; align-items: center; justify-content: center;">
            <div class="ui huge <%= color %> button" onclick="toggleStatus(this)">
                <% if (status === "home") { %>
                House is Empty
                <% } else { %>
                House is Occupied
                <% } %>
            </div>
        </div>
    </div>
</body>

</html>